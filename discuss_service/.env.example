# Database Configuration
DB_USERNAME=serp
DB_PASSWORD=serp
DB_URL=jdbc:postgresql://localhost:5432/serp_discuss_dev

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379

# Kafka Configuration
KAFKA_BOOTSTRAP_SERVERS=localhost:9092

# Keycloak Authentication
KEYCLOAK_URL=http://localhost:8180
CLIENT_SECRET=HNDdPOXuHKbg0Cfr0cfRD7SPtT2VOWhg

# Server Configuration
SERVER_PORT=8092

# ============================================
# Storage Configuration (MinIO/S3)
# ============================================

# Internal endpoint for server-to-MinIO communication
# - For Docker: Use Docker service name (e.g., http://serp-minio:9000)
# - For local development: Use localhost (e.g., http://localhost:9000)
# - For AWS S3: Leave empty (uses default AWS endpoint)
STORAGE_ENDPOINT=http://localhost:9000

# Public endpoint accessible by clients (browser, mobile app)
# - For Docker on same machine: Use host IP (e.g., http://192.168.1.100:9000)
# - For production: Use reverse proxy/CDN URL (e.g., https://s3.yourdomain.com)
# - If not set, falls back to STORAGE_ENDPOINT
STORAGE_PUBLIC_ENDPOINT=http://localhost:9000

# MinIO credentials (default values for local development)
# ⚠️ IMPORTANT: Change these in production!
STORAGE_ACCESS_KEY=minioadmin
STORAGE_SECRET_KEY=minioadmin123

# S3 bucket name for storing attachments
STORAGE_BUCKET=discuss-attachments

# AWS region (required by SDK, use us-east-1 for MinIO)
STORAGE_REGION=us-east-1

# Presigned URL expiry duration (in minutes) for on-demand download requests
PRESIGNED_URL_EXPIRY_MINUTES=60

# Download URL expiry duration (in days) for long-lived URLs in messages
DOWNLOAD_URL_EXPIRY_DAYS=7

# File upload limits
MAX_FILE_SIZE=52428800
MAX_FILES_PER_MESSAGE=10

# External Service URLs
ACCOUNT_SERVICE_URL=http://localhost:8081
