package serp.project.school_bus_service.enums;

public enum RoutePlanStudentAction {
    /** Student boards the bus at this stop (OUTBOUND direction) */
    BOARD,
    /** Student is dropped off at this stop (RETURN direction) */
    DROPOFF
}
